---
interface props {
   title: string;
   tagline?: string;
}
const { title, tagline } = Astro.props;
---

<div class="flex flex-col gap-2 px-4">
   <h1 class="text-4xl font-bold text-base-100">{title}</h1>
   {tagline && <p class="text-xl font-medium text-base-100">{tagline}</p>}
</div>
